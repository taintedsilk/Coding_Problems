#include <bits/stdc++.h>

using namespace std;

int main() {
	freopen("decor.inp", "r", stdin);
    freopen("decor.out", "w", stdout);
    unsigned long long n;
    cin >> n;
    unsigned long long a = floor( ((double)sqrt(n)));
    cout << fixed << a;}
